{% extends '@bolt/_base/_page-nav.twig' %}

{% block page_nav 'championship-menu' %}

{% block page_title __('general.phrase.championship.all') %}

{% block page_main %}
    {% import '@bolt/_macro/_macro.twig' as macro %}

    <div class="row">
        <div class="col-md-12">
            <div class="pull-right mb-md">
                <a class="btn btn-primary" href="{{ path('championshipedit', {'id': null}) }}">
                    <i class="fa fa-fw fa-plus"></i>
                    {{ __('panel.championship-actions.button.add') }}
                </a>
            </div>

            {{ include('@AsmbCompetition/championship/_list.twig') }}

            <hr class="invisible">

            <h2><strong>{{ __('general.phrase.team-list') }}</strong></h2>

            {{ include('@AsmbCompetition/championship/team/_list.twig') }}
        </div>
    </div>
{% endblock page_main %}
