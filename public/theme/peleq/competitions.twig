{% extends 'partials/_master.twig' %}

{% block main %}
    {% if record.tournoi_url_json is not empty %}
        {{ include('partials/competition/_tournament.twig') }}
    {% elseif record.competition_id > 0 %}
        {{ include('partials/competition/_championship.twig') }}
    {% elseif record.home_meetings_from_date is not empty and record.home_meetings_to_date is not empty %}
        {{ include('partials/competition/_home_meetings.twig') }}
    {% endif %}
{% endblock main %}
