{% form_theme form 'form_bolt_layout.twig' %}

<div class="panel panel-default">
    <div class="panel-heading">{{ __('panel.pool-actions.add') }}</div>
    <div class="panel-body">
        {{ form_start(form) }}
        <table class="table-condensed">
            <thead>
            <tr>
                <th class="col-xs-2"><label class="required"
                                            for="{{ form.category_name.vars.id }}">{{ __('general.phrase.category_name') }}</label>
                </th>
                <th class="col-xs-3"><label class="required"
                                            for="{{ form.name.vars.id }}">{{ __('general.phrase.name') }}</label></th>
                <th class="col-xs-2"><label
                            for="{{ form.division_fft_id.vars.id }}">{{ __('general.phrase.championship_fft_id') }}</label>
                </th>
                <th class="col-xs-2"><label
                            for="{{ form.division_fft_id.vars.id }}">{{ __('general.phrase.division_fft_id') }}</label>
                </th>
                <th class="col-xs-2"><label class="required"
                                            for="{{ form.fft_id.vars.id }}">{{ __('general.phrase.pool_fft_id') }}</label>
                </th>
                <th></th>
            </tr>
            </thead>

            <tbody>
            <tr>
                <td class="name">
                    {{ form_widget(form.category_name) }}
                </td>

                <td class="year">
                    {{ form_widget(form.name) }}
                </td>

                <td class="link_fft">
                    {{ form_widget(form.championship_fft_id) }}
                </td>

                <td class="link_fft">
                    {{ form_widget(form.division_fft_id) }}
                </td>

                <td class="link_fft">
                    {{ form_widget(form.fft_id) }}
                </td>

                <td class="actions">
                    {{ form_widget(form.save) }}
                </td>
            </tr>
            </tbody>
        </table>
        {{ form_end(form) }}
    </div>
    <div class="panel-body">
        <p class="text-muted">
            Si les résultats sont récupérés depuis la <strong>Gestion Sportive</strong>, seul l'ID de la
            <strong>poule</strong> est requis.
            S'ils sont récupérés depuis <strong>Ten'Up</strong>, l'ID du <strong>championnat</strong>, de la <strong>division</strong>
            et de la <strong>poule</strong>
            sont requis.
        </p>
    </div>
</div>
